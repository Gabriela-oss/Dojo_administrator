<%= form_with(model: course) do |form| %>
  <% if course.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(course.errors.count, "error") %> prohibited this course from being saved:</h2>

      <ul>
        <% course.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>


<div class="field">
    <%= form.label :dojo_id %>
    <%= form.select :dojo_id, options_from_collection_for_select(@dojos, :id, :name), prompt: 'Seleccionar el Dojo' %>
  </div>
  

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

    <div class="field">
    <%= form.label :sensei_courses_id %>
    <%= form.select :sensei_courses_id, options_from_collection_for_select(@senseis, :id, :name), prompt: 'Select a sensei' %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_field :description %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>

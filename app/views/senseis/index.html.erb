<h1>Senseis</h1>

<table>
  <thead>
    <tr>
      <th>Nombre</th>
      <th>Dojo</th>
      <th>ID</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @senseis.each do |sensei| %>
      <tr>
        <td><%= sensei.dojo.name %></td>
        <td><%= sensei.dojo.name %></td>
        <td><%= sensei.id_card %></td>
        <td><%= link_to sensei.name, senseis_show_path(sensei.id) %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%#= link_to 'Nuevo Curso', new_course_path %>